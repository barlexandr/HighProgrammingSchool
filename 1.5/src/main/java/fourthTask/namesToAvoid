Долго пытался найти примеры неправильных наименований, но не смог.

1.
    Тут нашел единственный пример, в котором используется тройка имен:
    PaymentService - сервис проведения оплаты.
    PaymentRestService - сервис, который выполняет вызов внешней системы для проведения оплаты.
    PaymentRestServiceInvoker - внутренний сервис, который оборачивает paymentRestService для подмены исключений.

    Названия вроде похожие, но выполняют разные функции и по имени можно увидеть.
    Сделана такая система из-за слишком больших абстракций, разросшегося проекта, сложных нефункциональных требований на каждый чих.

2.
    Цифры, разумеется, не использую в именованиях.

3.
    Однажды, в учебном проекте, использовал sum, max. Сейчас бы уточнял при наименовании, например, sumOfEvenNumbers.
    В рабочих проектах применяются большинство изложенных рекомендаций.

4.
    Избегаю неинформативных имен. Даю имена, несущие смысловую нагрузку, например, для List<Customer> customerList.

5.
    Не использую имена со скрытым смыслом.

6.
    Иногда именовал переменные с сокращенным вариантом avg (вместо average).
    Вообще на проектах часто встречаю сокращенное именование переменных, типа paymentDtTm.

7.
    Обычно, если дата указана в строке, то именую что-то в роде statusInstallationDateTime (указывая на тип даты в наименовании, и то, что это дата установки события.
    Более подробно не расписываю, так как это поле класса и класс сам задает основной контекст.)