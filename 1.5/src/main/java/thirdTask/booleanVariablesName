7.1
    trueBoolean - isClientValid
    // Является ли клиент валидным

    isEnabled - isQueueEnabled
    // Доступна ли очередь

    verdict - isTestCompletedCorrect
    // Признак решения теста

    editable - isPersonEditable
    // Есть ли возможность изменять объект person

    logError - isErrorLogging
    // Нужно ли логировать ошибки

7.2
    flag - done
    // Показатель того, что цикл завершен

    success
    // Как признак события об успешном выполнении операции при журналировании операций

    enabled
    // Как поле в фоновом процессе, которое будет показывать, активен ли процесс.
    // Если процесс по каким-то причинам будет отключен - в лог выведется об этом информация

7.3
    Да, в качестве индексов цикла используются преимущественно имена i,j, реже k.
    Нагляднее использовать более выразительное имя в том случае, если переменная-счетчик была создана не в блоке цикла (for), а до него и каким-то образом используется после, либо использовалась ранее.

7.4
    enabled - turnOff
    // Возможно использовать в случаях, когда выполняется работа с каким-либо процессом

    first - last
    // Возможно использовать при работе с коллекциями, когда необходимо знать первый и последний элемент.
    // Возможно, более применимо в каких-либо отсортированных коллекциях.

    begin - end
    // Возможно использовать при работе с циклами, которые запускаются при каком-то условии и с принудительным завершением

7.5
    Не нашел по коду ни 1 упоминания temp/var/.
    Использую временные переменные, которые необходимы для дальнейшего процесса, но получение которых неудобно (некрасиво, объемно, трудно читаемо) в блоках условий, либо аргументах функци.
    Либо,